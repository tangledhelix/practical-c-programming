#include <stdio.h>

/* Variable for computation results */
int answer;

int main() {
	answer = 2 + 2;
	/*printf( "The answer is %d\n" );*/
	printf( "The answer is %d\n", answer ); /* forgot variable in last line */

	return( 0 );
}
